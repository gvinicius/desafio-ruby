<%# navigation styled for Bootstrap 3.0 %>
<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <%= link_to 'RUBY CHALLENGE', root_path, class: 'navbar-brand' %>
      <% unless current_store.nil? %>
        <%=  current_store.name %>
        <%= image_tag current_store.logo.url unless current_store.logo_file_name.nil? %>
      <% end %>
      <ul class="nav navbar-nav">
      <% if store_signed_in? %>
        <li class="nav-item active"><%= link_to 'Admin', rails_admin_path %></li>
        <li class="nav-item active"><%= link_to 'Edit account', edit_store_registration_path %></li>
        <li class="nav-item active"><%= link_to 'Sign out', destroy_store_session_path, :method=>'delete' %></li>
      <% else %>
        <li class="nav-item active"><%= link_to 'Sign in', new_store_session_path %></li>
        <li class="nav-item active"><%= link_to 'Sign up', new_store_registration_path %></li>
      <% end %>
      </ul>
<br/><br/>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <%= render 'layouts/nav_links_for_auth' %>
      </ul>
    </div>
  </div>
</nav>
